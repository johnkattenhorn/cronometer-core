# Cronometer account credentials (both required). These are your normal
# cronometer.com website login — there is no separate API key.
CRONOMETER_USERNAME=
CRONOMETER_PASSWORD=

# Optional: override the Cronometer base URL (defaults to the public site).
# CRONOMETER_BASE_URL=https://cronometer.com
